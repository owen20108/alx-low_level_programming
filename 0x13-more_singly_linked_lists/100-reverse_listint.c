#include "lists.h"
/**
 * reverse_listint - reverse linked list of integers
 * @head: pointer to head
 * Return: pointer to head
 */

listint_t *reverse_listint(listint_t **head)
{
	head = head;

	return (*head);
}
